on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps: 
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6

      - name: Build App
        run: ./scripts/buildprod.sh

      - id: 'auth'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy
        run: gcloud builds submit --tag asia-south1-docker.pkg.dev/notely-505903/notely-ar-repo/notely:latest

      - name: Deploy to Cloud Run 
        run: gcloud run deploy notely --image asia-south1-docker.pkg.dev/notely-505903/notely-ar-repo/notely:latest--region asia-south1 --allow-unauthenticated --project notely-505903 --max-instances=4
